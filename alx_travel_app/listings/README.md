# Database Modeling and Data Seeding in Django

## Objective
The goal of this task is to define database models, create serializers for API data representation, and implement a management command to seed the database with sample data.

---

## Instructions

### 1. Duplicate Project
Duplicate the existing project `alx_travel_app` into a new project directory called `alx_travel_app_0x00`:

```bash
cp -r alx_travel_app alx_travel_app_0x00
cd alx_travel_app_0x00
